# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

dist-single/

# delivery artifacts
*.zip

# Android signing (ÄLÄ commitoi näitä)
*.jks
*.keystore
android/keystore.properties
android/app/release/

# Supabase CLI local cache
supabase/.temp/

# Environment files (.env is currently tracked in git history — run
# `git rm --cached .env` once to stop tracking it going forward; the
# VITE_SUPABASE_PUBLISHABLE_KEY in it is a public anon key by Supabase's own
# design, but committed env files are still bad practice in general).
.env
.env.local
.env.*.local
